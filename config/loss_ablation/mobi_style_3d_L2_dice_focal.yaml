extends: ../Proposal/mobi_style_3d.yaml

project:
  # Output: outputs/loss_ablation/L2_dice_focal/mobi_style_3d/..._epoch60_L2
  output_root: outputs/loss_ablation/L2_dice_focal
  output_suffix: L2

training:
  # L2 = Dice + Focal.
  loss:
    name: dice_focal
    focal_alpha: 0.25
    focal_gamma: 2.0
